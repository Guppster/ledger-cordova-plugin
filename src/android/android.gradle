repositories{    
    jcenter()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    //for AAR in /libs
    //compile(name:'AARName', ext:'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
